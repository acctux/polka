# example configuration file for khard version > 0.14.0
# place it under ~/.config/khard/khard.conf
# This file is parsed by the configobj library.  The syntax is described at
# https://configobj.readthedocs.io/en/latest/configobj.html#the-config-file-format
[addressbooks]
[[family]]
path = ~/Desktop/Contacts/family/
[[friends]]
path = ~/Desktop/Contacts/friends/
[[work]]
path = ~/Desktop/Contacts/work/**/client-*-contacts/
type = discover
[general]
debug = no
default_action = list
# These are either strings or comma seperated lists
editor = nvim
merge_editor = nvim -d

[contact table]
# display by: first_name / last_name / formatted_name
display = first_name
group_by_addressbook = yes
reverse = no
show_nicknames = yes
sort_by = last_name
show_uids = no
show_kinds = yes
localize_dates = yes
# set a comma separated list of preferred phone number types in descending priority
# or nothing for non-filtered alphabetical order
preferred_phone_number_type = pref, cell, home
# set a comma separated list of preferred email address types in descending priority
# or nothing for non-filtered alphabetical order
preferred_email_address_type = pref, work, home

[vcard]
# extend contacts with your own private objects
# these objects are stored with a leading "X-" before the object name in the vcard files
# every object label may only contain letters, digits and the - character
# default: ,  (the empty list)
private_objects = Skype, Twitter
# preferred vcard version: 3.0 / 4.0
preferred_version = 4.0
# Look into source vcf files to speed up search queries: yes / no
search_in_source_files = no
# skip unparsable vcard files: yes / no
skip_unparsable = no
