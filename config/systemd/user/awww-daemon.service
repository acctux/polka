[Unit]
Description=awww wallpaper daemon
After=graphical-session.target
PartOf=graphical-session.target

[Service]
ExecStart=/usr/sbin/awww-daemon
Restart=on-failure
Type=simple

[Install]
WantedBy=graphical-session.target
